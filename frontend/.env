# =============================================================================
# MoStar Grid - Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env or .env.local to git!

# -----------------------------------------------------------------------------
# DATABASE (Required for production, optional for dev with ALLOW_NO_DB=true)
# -----------------------------------------------------------------------------
# Neon PostgreSQL connection string (sslmode=require for production)
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Allow running without database (dev only)
ALLOW_NO_DB=true

# -----------------------------------------------------------------------------
# API SERVER
# -----------------------------------------------------------------------------
# Host and port for FastAPI (use 0.0.0.0 for containers/cloud)
API_HOST=127.0.0.1
API_PORT=8000

# Grid operating mode (development | production)
GRID_MODE=development

# -----------------------------------------------------------------------------
# CORS (Production)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# Example: https://mo-star-grid.vercel.app,https://*.vercel.app
ALLOW_ORIGINS=*

# -----------------------------------------------------------------------------
# FRONTEND (Vite Environment Variables)
# -----------------------------------------------------------------------------
# These are prefixed with VITE_ so they're exposed to the browser

# Gemini API Key (for client-side AI features)
VITE_API_KEY=AIzaSyANT-4Tmc1P4RjUjam5tgEDQ4LLUAwzu8Q

# Production Backend URLs (leave empty for dev - uses Vite proxy)
# VITE_API_URL=https://api.mostar-grid.xyz/api
# VITE_WS_URL=wss://api.mostar-grid.xyz/ws

# -----------------------------------------------------------------------------
# OPTIONAL: Provenance Generation
# -----------------------------------------------------------------------------
# Base directory for provenance artifacts (defaults to backend/data)
# MOSTAR_PROV_BASE=/custom/path/to/data

# -----------------------------------------------------------------------------
# MOSTLYAI SYNTHETIC DATA GENERATOR
# -----------------------------------------------------------------------------
# Your MostlyAI API credentials for synthetic data generation
MOSTLY_API_KEY=mostly-3f1f96f1bef2c8acdffeb9d0d37ab7c47d5e07cebe47a2370124a470e49b0f31
MOSTLY_BASE_URL=https://app.mostly.ai
MOSTLY_GENERATOR_ID=22df93d3-bd0d-4857-ba69-0653249ddfd4
