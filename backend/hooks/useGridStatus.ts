// This hook has been replaced by the more advanced useGridStream hook,
// which uses a WebSocket for real-time updates instead of polling.
// The file is kept to avoid breaking imports in older states but is no longer used.
